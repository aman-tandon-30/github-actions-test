name: Test revision

on: [push]

jobs:
  test-revison:
    runs-on: ubuntu-latest
    steps:
      - name: Printing the directory files, if any
        run:  |
          git --version
          ls
          cd $GITHUB_WORKSPACE
          git checkout $GITHUB_SHA
